<%- include("./partials/header.ejs") %>
<main>
    <h1>WOW it works</h1>
    <% if (posts.length > 0) { %>
    <% posts.forEach(post => { %>
    <div>
        <h2><%= post.title %> </h2>
        <p><%= post.content %> </p>
        <p><%= post.author %> on <%= post.updated %></p>
    </div>
    <% }) %>
    <% } %>
</main>
<%- include("./partials/footer.ejs") %>